name: Deploy documentation to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - "docs/**"
      - "fr_FR/**"
      - "index.md"
      - "_config.yml"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare site
        run: |
          set -euo pipefail
          rm -rf _site
          mkdir -p _site

          cp -a fr_FR _site/fr_FR
          cp -a index.md _site/index.md
          cp -a _config.yml _site/_config.yml

          test -f _site/fr_FR/index.md
          test -f _site/fr_FR/changelog/index.md
          grep -q 'v1.0.42' _site/fr_FR/index.md
          grep -q 'v1.0.42' _site/fr_FR/changelog/index.md

          if grep -R -i -E 'MQTT|Apache|PROD|DEV valid|DEV validée|DEV validee|non touch|redémarr|redemarr' _site; then
            echo "Internal words found in public documentation"
            exit 1
          fi

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "_site"

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
